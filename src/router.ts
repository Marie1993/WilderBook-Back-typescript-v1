// // Routes
// import expressRouter from 'express';

// import asyncHandler from 'express-async-handler';

// import wilders from './controllers/wildersController';

// const router = expressRouter.Router();

// router.post('/api/wilder/create', asyncHandler(wilders.create));
// router.get('/api/wilder/:id', asyncHandler(wilders.read));
// router.get('/api/wilder', asyncHandler(wilders.readAll));
// router.post('/x', asyncHandler(wilders.filterLte));
// router.put('/api/wilder/update/:id', asyncHandler(wilders.update));
// router.delete('/api/wilder/delete/:id', asyncHandler(wilders.delete));

// export default router;
